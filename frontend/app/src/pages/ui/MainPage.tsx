import type { FC } from "react";

const MainPage: FC<{}> = () => {
	return (
		<div style={{ height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
			Game(Main) Page is not ready yet!
		</div>
	)
}

export default MainPage