import type { FC } from "react";

const NewsPage: FC<{}> = () => {
	return (
		<div style={{ position: 'relative', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
			News Page is not ready yet!
		</div>
	)
}

export default NewsPage