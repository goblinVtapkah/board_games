import type { FC } from "react"

const ProfilePage: FC<{}> = () => {
	return (
		<div style={{ position: 'relative', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
			Profile Page is not ready yet!
		</div>
	)
}

export default ProfilePage